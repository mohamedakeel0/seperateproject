

enum Services { New, Old, Initial }

enum Toaststates { SUCCESS, ERROR, WARNING }

enum BillTypes { SELLING_BILL, RETURN_BILL, OFFER_PRICE, DELIVERY_ORDERS }

enum TransactionsTypes { TR_COLLECT, TR_PAY }

enum RequestState {
  loading,
  loaded,
  error,
}